/*
FEATURE:  Add User

SCENARIO: Adding a user
  GIVEN:  I provide valid user data
  WHEN:   I try to add user
  THEN:   User is saved successfully

SCENARIO: Invalid password
  GIVEN:  I provide an invalid password
  WHEN:   I try to add user
  THEN:   I should get an invalid password error
*/

test.todo("Invalid password");

describe("SCENARIO: Adding a user", () => {
  describe("GIVEN: I provide valid user data", () => {
    test.todo("WHEN: I try to add user; THEN: user is saved successfully.");
  });
});

describe("SCENARIO: Invalid password", () => {
  describe("GIVEN: I provide an invalid password", () => {
    test.todo(
      "WHEN: I try to add user; THEN: I should get an invalid password error."
    );
  });
});
